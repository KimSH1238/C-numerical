#include <stdio.h>

int calcul(void)
{
	int a = 3;
	int b = 2;
	printf("결과값=%d\n", a + b);
	printf("결과값=%d\n", a - b);
	printf("결과값=%d\n", a * b);
	printf("결과값=%d\n", a / b);
	
	return 0;
}